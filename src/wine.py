# -*- coding: utf-8 -*-
"""Wine.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/11KspaKBj9boDRhWMCDjxmHYBGkAXutU1
"""

from sklearn.model_selection import train_test_split
from sklearn.datasets import load_wine
from sklearn.metrics import classification_report
from sklearn.svm import SVC
from sklearn.preprocessing import StandardScaler
from google.colab import files
import numpy as np
import pandas as pd
import joblib

#instanciando o scaler e o classificador
svmModel = SVC(kernel="rbf", C=0.1, gamma="auto")
scaler = StandardScaler()

#carregando e processando o dataset
wine = load_wine()
X, y = wine.data, wine.target
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=3)

X_train_scaled = scaler.fit_transform(X_train)

#visualização tabular dos dados

data = pd.DataFrame(data=X, columns=wine.feature_names)
data["target"] = wine.target
data["target name"] = pd.Categorical.from_codes(y, wine.feature_names)
# data #use se estiver usando em um notebook jupyter ou colab

#treinando o SVM  e exibindo os resultados

svmModel.fit(X_train_scaled, y_train)
X_test_scaled = scaler.transform(X_test)
y_pred = svmModel.predict(X_test_scaled)
resultados = classification_report(y_test, y_pred)
print(resultados)

joblib.dump(svmModel, "svmWine.pkl")
joblib.dump(scaler, "scalerWine.pkl")

files.download("svmWine.pkl")
files.download("scalerWine.pkl")
